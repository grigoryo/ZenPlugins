import * as index from './index'

describe('unit tests', () => {
  it('TODO: write tests', async () => {
    throw new Error('!')
  })
})

describe('integration tests', () => {
  it('TODO: write tests', async () => {
    throw new Error('!')
  })
})
